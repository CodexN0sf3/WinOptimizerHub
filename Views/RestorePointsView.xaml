<UserControl x:Class="WinOptimizerHub.Views.RestorePointsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:WinOptimizerHub.Converters">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolVis"/>
        <converters:InverseBoolConverter x:Key="InvBool"/>
    </UserControl.Resources>
    <StackPanel Margin="24">
        <Grid Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Style="{DynamicResource HeadingText}" Text="System Restore Points"/>
                <TextBlock Style="{DynamicResource SecondaryText}" Text="Manage Windows System Restore points" Margin="0,4,0,0"/>
            </StackPanel>
            <Button Grid.Column="1" Style="{DynamicResource SecondaryButton}" Command="{Binding RefreshCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InvBool}}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE72C;" FontFamily="{DynamicResource IconFont}" FontSize="14" Margin="0,0,8,0"/>
                    <TextBlock Text="Refresh"/>
                </StackPanel>
            </Button>
            <Button Grid.Column="3" Style="{DynamicResource SuccessButton}" Command="{Binding CreateCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InvBool}}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE72B;" FontFamily="{DynamicResource IconFont}" FontSize="14" Margin="0,0,8,0"/>
                    <TextBlock Text="Create Point"/>
                </StackPanel>
            </Button>
            <Button Grid.Column="5" Style="{DynamicResource DangerButton}" Command="{Binding DeleteCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InvBool}}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE74D;" FontFamily="{DynamicResource IconFont}" FontSize="14" Margin="0,0,8,0"/>
                    <TextBlock Text="Delete Selected"/>
                </StackPanel>
            </Button>
        </Grid>

        <Border Style="{DynamicResource Card}"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolVis}}"
                Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="{Binding StatusMessage}" Style="{DynamicResource BodyText}" Margin="0,0,0,8"/>
                <ProgressBar Style="{DynamicResource ModernProgressBar}" IsIndeterminate="True"/>
            </StackPanel>
        </Border>

        <Border Style="{DynamicResource Card}">
            <ListView Style="{DynamicResource ModernListView}" ItemsSource="{Binding Points}" MaxHeight="480" ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Width="36">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" Style="{DynamicResource ModernCheckBox}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="#" Width="50" DisplayMemberBinding="{Binding SequenceNumber}"/>
                        <GridViewColumn Header="Description" Width="260" DisplayMemberBinding="{Binding Description}"/>
                        <GridViewColumn Header="Created" Width="180" DisplayMemberBinding="{Binding FormattedDate}"/>
                        <GridViewColumn Header="Type" Width="180" DisplayMemberBinding="{Binding TypeDisplay}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    </StackPanel>
</UserControl>
